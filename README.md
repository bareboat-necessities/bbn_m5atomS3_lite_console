# bbn_m5atomS3_lite_console
Console on esp32 example

## Console

Compile with Arduino Release v3.3.5 based on ESP-IDF v5.5.1+ with USB CDC boot and Hardware CDC options.

Use PUTTY as terminal app to connect via USB serial (COM<X> on windows or /dev/tty<ID> on linux) with 115200 baud rate.

